修正履歴
Var 1.5.0 on 2016/6/
・installコマンドを追加しました。HSPPOを別の場所にインストールして動作させられます。
  ・ダウンロードしたZIPを展開した時点ではゲストモードとなり、ツアーが開始されるほか、ROOTの書き換えができなくなります。(1.5.0の段階では一応可能)
・hsppotourコマンドを追加しました。ゲストモードのときはツアーができます。通常は何もしません。
・valueppコマンドを追加しました。環境変数の値を1大きくします。
・valueddコマンドを追加しました。環境変数の値を1小さくします。
・goto命令を追加しました。label命令で定義されたラベルにジャンプします。
・exist命令を追加しました。ファイルの存在の有無を確認してファイルサイズを環境変数@existに代入します。
・existfolder命令を追加しました。フォルダの存在の有無を確認してファイルサイズを環境変数@existに代入します。
・cd命令で/drives/の中を指定すると絶対パスにアクセスできるようになりました。
・cd命令で最後に/や\が入っていても成功するようになりました。
・getfilelist命令を実装しました。ファイルリストを任意の環境変数に代入します。
・getfolderlist命令を実装しました。フォルダリストを任意の環境変数に代入します。
・上2の命令実装に伴い、ls命令をスクリプト実装化しました。
・modinitコマンドを追加しました。将来mod機能やhappを実装したときの初期化処理を担当します。
・modcloseコマンドを追加しました。将来mod機能やhappを実装したときの終了処理を担当します。
・value.asにパーミッション機能を付加しました。環境変数のパーミッションコードが指定されたもの以上ならば書き込みができます。また、存在しない環境変数でパーミッションコードが指定された場合はそれを環境変数のパーミッションとします。また。権限があればパーミッションコードを書き換える命令も追加しました。
・スクリプトに引数を渡すために、スクリプトが呼び出されたときのみに代入される、@ASCCALL1～@ASCCALL32 までの環境変数を新たに定義しました。
・ルートファイルシステムの構造を変更しました。ROOT 外にあった全てのフォルダ、ファイルをROOT フォルダの中に置きました。
・xcopyコマンドを実装しました。絶対パスで指定するとそのフォルダ内の全てのファイルをコピーできます。
・環境変数echo.backcolor にblack などの色コードを代入すると、次にecho命令を実行したときに背景色を変えられます。仮実装として5色を定義しています。


Var 1.4.1 on 2016/5/10
・commandsフォルダの中の.asをすべてsourcesフォルダに移動しました。
・setコマンドを実装しました。setvalueと同じように使えます。これはPHPPO互換性向上の為です。

Var 1.4.0 on 2016/05/5
・aevalueコマンドを実装しました。環境変数に任意の文字列を追加できます。
・スクリプト中でも他のスクリプトを呼び出すときに引数を渡せるようにしました。
・cmpif命令を追加しました。二つの文字列が同じときのみ次の行の命令を実行します。
・input命令を追加しました。ユーザーが打った文字列を改行まで環境変数に読み出します。
・rem命令を追加しました。何もしません!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
・label命令を追加しました。何もしません!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
・2つ目の項目の修正によるトラブルに対応するために、プロンプトからの引数を%@A*%に保持するようにしました。
・prompt命令を追加しました。プロンプトを対話型に設定できます。setvalue時と異なり環境変数が反映されないので注意してください。

Var.1.3.1b on 2016/4/15
・brコマンドのバグを修正しました。
・gettime getdate命令を新設し、改行するかはtimeスクリプト、dateスクリプトに任せるようにしました。

Var.1.3.0b on 2016/04/12
・環境変数機能を実装しました。また、prompt、doingなどの変数を環境変数化しました。これにより簡単に書き換えることができます。
　・setvalueコマンドを実装しました。
・コンソール表示機構をモジュール化しました。ソースの可読性が向上しました。
・環境変数を利用できるようになりました。%prompt%などのようにすると利用できます。
　・これによりexit命令がスクリプトになりました。時間指定がミリ秒指定となりました。
・新たに @ キーが打てるようになりました。
・環境変数echo.autoreturnによりecho後に自動的に改行が入るかどうかを指定できるようになりました。br命令では従来通り必ず改行が入ります。
・BEGIN.shの内容を起動時に実行するようになりました。



Var.1.2.0b on 2016/02/09
・一部の使用しない命令を、system'を先頭に付加して、システムの隠し命令のようにしました。(reset命令)
・goline コマンドを追加しました。スクリプト内で行番号を指定すればその行にジャンプすることができます。
・scriptの1重ネストを可能にしました。
・brコマンドを追加しました。改行することを示す命令で、echo よりわかりやすくなると思います。
・\:の2つの記号を打てるようにしました。
・scriptコマンド実行中に、ENDキーを押せばスクリプトの実行を停止するようにしました。ただし、wait命令実行中はできないようです。
・擬似ROOT機能を実装しました。bin フォルダ内には外部コマンドが格納されています。
・lsコマンドを追加しました。引数指定はできませんが、コマンドプロンプトのdir命令とほぼ同じです。
・verコマンドをスクリプト実装で追加しました。コマンドプロンプトのver命令とほぼ同じです。
・catコマンドを追加しました。コマンドプロンプトのtype命令とほぼ同じです。

Var.1.1.3b on 2016/01/25
・waitコマンドを追加しました。ミリ秒単位で実行を停止させることができます。

Var.1.1.2b on 2016/01/19
・scriptsフォルダに入っているスクリプトを "script" 命令なしで実行できるようにしました。
・それにより、help、clsなどのコマンドがバッチ化されました。

Var.1.1.1b on 2016/01/18
・scriptコマンドでファイルが存在しないときにUnknown command と出るのバグを改善しました。

Var.1.1.0b on 2016/01/17
・execコマンドで実行ファイルが存在しないときにエラーで強制終了するのを改善しました。それにより絶対パスの指定が必須になったものの、現在cmd,calc,notepadなどは現在絶対パスでなくても可能です。
・非アクティブウィンドウ時にキー入力を受け取るバクを改善しました。
・スクリプト命令を追加しました。script の後に半角スペースを置いてファイル名を指定するとそのファイルの内容を実行します。
・ソースコードの一部を#include 化させることによりソースコードの可読性が向上しました。
・. / の他に ! " # $ % & '( )の、合わせて11の記号を打てるようにしました。